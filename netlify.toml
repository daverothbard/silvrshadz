[build]
  command = "hugo"
  publish = "public"

[build.environment]
  HUGO_VERSION = "0.155.3"
```

Save, then push again:
```
cd ~\Documents\silvrshadz
```
```
git add -A
```
```
git commit -m "Fix netlify config encoding"
```
```
git push